class ItemListModel {
  final String itemId, iconId, classifyType;
  final int rarity, sortId;

  ItemListModel({
    required this.itemId,
    required this.iconId,
    required this.classifyType,
    required this.rarity,
    required this.sortId,
  });
}
